# deps / build
node_modules/
dist/
*.tsbuildinfo
coverage/

# secrets
.env
.env.*
!.env.example

# dataset outputs (rebuildable via `pnpm dataset:build`)
data/exports/
data/raw/
*.pglite/

# blinded ambiguity study: rebuildable via `pnpm --filter @verdict/data run
# blind-study` (deterministic seed). Not committed for two reasons — key.json
# would publish the answers next to the questions, and ambiguity-study.json
# carries 170 markets' verbatim rules text, which SEO.md §4 says we don't
# republish wholesale.
data/blind/

# python
eval/.venv/
__pycache__/
*.pyc
eval/.ipynb_checkpoints/

# foundry
contracts/out/
contracts/cache/
contracts/broadcast/

# misc
.DS_Store
data/postmortems/
