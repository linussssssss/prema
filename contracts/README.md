# contracts

Foundry project, Phase 3 scope (`EvidenceRegistry.sol` on Base; Base Sepolia
for testing). Phase 0 ships only a stub so the repo layout and toolchain slot
are fixed. Not built in CI yet; `forge build` requires a local Foundry install.
